<UserControl x:Class="rntlOS.BackOffice.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="Gestion des Clients" 
                   Foreground="White"
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Bouton Ajouter -->
        <Button Grid.Row="1"
                Content="+ Ajouter un client"
                Click="AjouterClient_Click"
                Background="#007ACC"
                Foreground="White"
                Padding="15,8"
                FontSize="14"
                Margin="0,0,0,10"
                HorizontalAlignment="Left"
                Cursor="Hand"/>

        <!-- DataGrid -->
        <DataGrid x:Name="ClientsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Telephone}" Width="*"/>
                <DataGridTextColumn Header="Date d'inscription" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>

                <!-- Colonne Actions -->
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️ Modifier"
                                        Click="ModifierClient_Click"
                                        Background="#FFC107"
                                        Foreground="White"
                                        Padding="8,4"
                                        Margin="0,0,5,0"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                                <Button Content="🗑️ Supprimer"
                                        Click="SupprimerClient_Click"
                                        Background="#DC3545"
                                        Foreground="White"
                                        Padding="8,4"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>