<UserControl x:Class="rntlOS.BackOffice.Views.BookingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="Gestion des Réservations" 
                   Foreground="White"
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Bouton Ajouter -->
        <Button Grid.Row="1"
                Content="+ Nouvelle réservation"
                Click="AjouterReservation_Click"
                Background="#007ACC"
                Foreground="White"
                Padding="15,8"
                FontSize="14"
                Margin="0,0,0,10"
                HorizontalAlignment="Left"
                Cursor="Hand"/>

        <!-- DataGrid -->
        <DataGrid x:Name="BookingsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Client" Binding="{Binding Client.Prenom}" Width="*"/>
                <DataGridTextColumn Header="Nom Client" Binding="{Binding Client.Nom}" Width="*"/>
                <DataGridTextColumn Header="Véhicule" Binding="{Binding Vehicule.Marque}" Width="*"/>
                <DataGridTextColumn Header="Modèle" Binding="{Binding Vehicule.Modele}" Width="*"/>
                <DataGridTextColumn Header="Date Début" Binding="{Binding DateDebut, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                <DataGridTextColumn Header="Date Fin" Binding="{Binding DateFin, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                <DataGridTextColumn Header="Prix Total" Binding="{Binding PrixTotal, StringFormat={}{0:C}}" Width="*"/>
                <DataGridTextColumn Header="Statut" Binding="{Binding Status}" Width="*"/>

                <DataGridTemplateColumn Header="Paiement" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="💳 Payer"
                    Click="GererPaiement_Click"
                    Background="#28A745"
                    Foreground="White"
                    Padding="8,4"
                    Cursor="Hand"
                    BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                

                <!-- Colonne Actions -->
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️ Modifier"
                                        Click="ModifierReservation_Click"
                                        Background="#FFC107"
                                        Foreground="White"
                                        Padding="8,4"
                                        Margin="0,0,5,0"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                                <Button Content="🗑️ Supprimer"
                                        Click="SupprimerReservation_Click"
                                        Background="#DC3545"
                                        Foreground="White"
                                        Padding="8,4"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>