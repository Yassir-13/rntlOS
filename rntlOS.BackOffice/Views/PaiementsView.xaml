<UserControl x:Class="rntlOS.BackOffice.Views.PaiementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="Gestion des Paiements" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,20"/>

        <!-- Statistiques rapides -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                    Background="#28A745" 
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Total Payé" 
                               Foreground="White" 
                               FontSize="14"
                               Opacity="0.9"/>
                    <TextBlock x:Name="TxtTotalPaye" 
                               Text="0.00 MAD" 
                               Foreground="White" 
                               FontSize="28"
                               FontWeight="Bold"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" 
                    Background="#FFC107" 
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="En Attente" 
                               Foreground="White" 
                               FontSize="14"
                               Opacity="0.9"/>
                    <TextBlock x:Name="TxtEnAttente" 
                               Text="0.00 MAD" 
                               Foreground="White" 
                               FontSize="28"
                               FontWeight="Bold"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" 
                    Background="#DC3545" 
                    CornerRadius="8"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="Échoué" 
                               Foreground="White" 
                               FontSize="14"
                               Opacity="0.9"/>
                    <TextBlock x:Name="TxtEchoue" 
                               Text="0.00 MAD" 
                               Foreground="White" 
                               FontSize="28"
                               FontWeight="Bold"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- DataGrid -->
        <DataGrid x:Name="PaiementsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Client" Binding="{Binding Booking.Client.Prenom}" Width="*"/>
                <DataGridTextColumn Header="Nom" Binding="{Binding Booking.Client.Nom}" Width="*"/>
                <DataGridTextColumn Header="Véhicule" Binding="{Binding Booking.Vehicule.Marque}" Width="*"/>
                <DataGridTextColumn Header="Montant" Binding="{Binding Montant, StringFormat={}{0:C}}" Width="*"/>
                <DataGridTextColumn Header="Méthode" Binding="{Binding Methode}" Width="*"/>
                <DataGridTextColumn Header="Statut" Binding="{Binding Statut}" Width="*"/>
                <DataGridTextColumn Header="Date" Binding="{Binding DatePaiement, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="*"/>

                <!-- Colonne Actions -->
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️ Modifier"
                                        Click="ModifierPaiement_Click"
                                        Background="#FFC107"
                                        Foreground="White"
                                        Padding="8,4"
                                        Margin="0,0,5,0"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                                <Button Content="🗑️ Supprimer"
                                        Click="SupprimerPaiement_Click"
                                        Background="#DC3545"
                                        Foreground="White"
                                        Padding="8,4"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>