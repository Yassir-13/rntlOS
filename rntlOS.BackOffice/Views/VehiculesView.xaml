<UserControl x:Class="rntlOS.BackOffice.Views.VehiculesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="Gestion des Véhicules"
                   Foreground="White"
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Bouton Ajouter -->
        <Button Grid.Row="1"
                Content="+ Ajouter un véhicule"
                Click="AjouterVehicule_Click"
                Background="#007ACC"
                Foreground="White"
                Padding="15,8"
                FontSize="14"
                Margin="0,0,0,10"
                HorizontalAlignment="Left"
                Cursor="Hand"/>

        <!-- DataGrid -->
        <DataGrid x:Name="VehiculesDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Marque" Binding="{Binding Marque}" Width="*"/>
                <DataGridTextColumn Header="Modèle" Binding="{Binding Modele}" Width="*"/>
                <DataGridTextColumn Header="Matricule" Binding="{Binding Matricule}" Width="*"/>
                <DataGridTextColumn Header="Prix/Jour" Binding="{Binding PrixParJour, StringFormat={}{0:C}}" Width="*"/>
                <DataGridCheckBoxColumn Header="Disponible" Binding="{Binding Disponible}" Width="Auto"/>
                <DataGridTextColumn Header="Kilométrage" Binding="{Binding Kilometrage, StringFormat={}{0:N0}}" Width="*"/>

                <!-- Colonne Actions -->
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️ Modifier"
                                        Click="ModifierVehicule_Click"
                                        Background="#FFC107"
                                        Foreground="White"
                                        Padding="8,4"
                                        Margin="0,0,5,0"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                                <Button Content="🗑️ Supprimer"
                                        Click="SupprimerVehicule_Click"
                                        Background="#DC3545"
                                        Foreground="White"
                                        Padding="8,4"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>