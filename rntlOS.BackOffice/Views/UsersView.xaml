<UserControl x:Class="rntlOS.BackOffice.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="Gestion des Employés" 
                   Foreground="White"
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Bouton Ajouter -->
        <Button Grid.Row="1"
                Content="+ Ajouter un employé"
                Click="AjouterUser_Click"
                Background="#007ACC"
                Foreground="White"
                Padding="15,8"
                FontSize="14"
                Margin="0,0,0,10"
                HorizontalAlignment="Left"
                Cursor="Hand"/>

        <!-- DataGrid -->
        <DataGrid x:Name="UsersDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Rôle" Binding="{Binding Role}" Width="*"/>
                <DataGridCheckBoxColumn Header="Actif" Binding="{Binding IsActive}" Width="Auto"/>
                <DataGridTextColumn Header="Date création" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>

                <!-- Colonne Actions -->
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️ Modifier"
                                        Click="ModifierUser_Click"
                                        Background="#FFC107"
                                        Foreground="White"
                                        Padding="8,4"
                                        Margin="0,0,5,0"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                                <Button Content="🗑️ Supprimer"
                                        Click="SupprimerUser_Click"
                                        Background="#DC3545"
                                        Foreground="White"
                                        Padding="8,4"
                                        Cursor="Hand"
                                        BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>