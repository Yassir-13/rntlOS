@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            <a href="/" class="ms-md-auto">
                <span style="font-weight: 300;">rntl</span><strong style="color: #D4AF37;">OS</strong>
            </a>
        </div>

        <article class="content px-4">
            @Body
        </article>

        <Footer />
    </main>
</div>

<style>
    @@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;600;700&family=Montserrat:wght@300;400;500;600;700&display=swap');

    :root {
        --gold: #C9A961;
        --dark-navy: #0B1A2E;
        --light-navy: #152238;
        --cream: #FAF9F6;
        --silver: #B8B8B8;
    }

    * {
        font-family: 'Montserrat', sans-serif;
        margin: 0;
        padding: 0;
    }

    h1, h2, h3, h4, h5, h6 {
        font-family: 'Cormorant Garamond', serif;
        font-weight: 600;
    }

    body {
        background: var(--cream);
    }

    /* ========== SIDEBAR PRESTIGE ========== */
    .sidebar {
        background: linear-gradient(180deg, var(--dark-navy) 0%, #000000 100%);
        border-right: 2px solid var(--gold);
        position: relative;
        overflow: hidden;
    }

        .sidebar::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 1px;
            height: 100%;
            background: linear-gradient(180deg, transparent 0%, var(--gold) 50%, transparent 100%);
            box-shadow: 0 0 20px var(--gold);
        }

    .top-row {
        background: rgba(201, 169, 97, 0.03);
        border-bottom: 1px solid rgba(201, 169, 97, 0.3);
        backdrop-filter: blur(10px);
        padding: 2rem 1.5rem !important;
    }

    .navbar-brand {
        font-family: 'Cormorant Garamond', serif !important;
        font-size: 2.2rem !important;
        font-weight: 600 !important;
        letter-spacing: 3px !important;
        text-transform: uppercase;
        background: linear-gradient(135deg, white 0%, var(--gold) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    /* ========== NAVIGATION ÉLÉGANTE ========== */
    .nav-item {
        margin: 8px 20px;
        position: relative;
    }

    .nav-link {
        border-radius: 0;
        padding: 16px 24px !important;
        transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        color: rgba(255, 255, 255, 0.7) !important;
        font-weight: 400;
        letter-spacing: 1.5px;
        text-transform: uppercase;
        font-size: 0.85rem;
        border-left: 2px solid transparent;
        position: relative;
        overflow: hidden;
    }

        .nav-link::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 0;
            background: linear-gradient(90deg, rgba(201, 169, 97, 0.2) 0%, transparent 100%);
            transition: width 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .nav-link:hover::before {
            width: 100%;
        }

        .nav-link:hover {
            color: var(--gold) !important;
            border-left-color: var(--gold);
            padding-left: 32px !important;
            background: rgba(201, 169, 97, 0.05);
        }

        .nav-link.active {
            background: linear-gradient(90deg, rgba(201, 169, 97, 0.15) 0%, transparent 100%);
            color: var(--gold) !important;
            border-left: 2px solid var(--gold);
            font-weight: 500;
            box-shadow: inset 0 0 20px rgba(201, 169, 97, 0.1);
        }

            .nav-link.active::after {
                content: '';
                position: absolute;
                right: 0;
                top: 50%;
                transform: translateY(-50%);
                width: 4px;
                height: 40%;
                background: var(--gold);
                box-shadow: 0 0 10px var(--gold);
            }

    /* ========== MAIN CONTENT LUXUEUX ========== */
    main {
        background: var(--cream);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
    }

        main > .top-row {
            background: white;
            border-bottom: 1px solid #E5E5E5;
            padding: 2rem 3rem !important;
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.03);
        }

            main > .top-row a {
                font-family: 'Cormorant Garamond', serif;
                font-size: 1.8rem;
                font-weight: 600;
                letter-spacing: 2px;
            }

    .content {
        padding: 3rem;
    }

    /* ========== CARDS PREMIUM ========== */
    .card {
        border: none;
        border-radius: 0;
        box-shadow: 0 5px 30px rgba(0, 0, 0, 0.08);
        transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        background: white;
        overflow: hidden;
        position: relative;
    }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--gold) 0%, transparent 100%);
            opacity: 0;
            transition: opacity 0.5s;
        }

        .card:hover {
            box-shadow: 0 15px 50px rgba(11, 26, 46, 0.15);
            transform: translateY(-8px);
        }

            .card:hover::before {
                opacity: 1;
            }

    .card-title {
        font-family: 'Cormorant Garamond', serif;
        color: var(--dark-navy);
        font-weight: 600;
        font-size: 1.75rem;
        margin-bottom: 1.5rem;
        letter-spacing: 1px;
    }

    .card-header {
        background: linear-gradient(135deg, var(--dark-navy) 0%, #000000 100%);
        color: white;
        border: none;
        padding: 2rem;
        font-weight: 400;
        border-bottom: 3px solid var(--gold);
    }

    .card-body {
        padding: 2.5rem;
        background: white;
    }

    .card-footer {
        background: #FAFAFA;
        border-top: 1px solid #E5E5E5;
        padding: 1.5rem;
    }

    /* ========== BOUTONS PRESTIGE ========== */
    .btn {
        border-radius: 0;
        padding: 14px 40px;
        font-weight: 500;
        border: none;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        font-size: 0.9rem;
        position: relative;
        overflow: hidden;
    }

        .btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn:hover::before {
            width: 300px;
            height: 300px;
        }

    .btn-primary {
        background: linear-gradient(135deg, var(--dark-navy) 0%, #000000 100%);
        color: var(--gold);
        border: 2px solid var(--gold);
        box-shadow: 0 0 20px rgba(201, 169, 97, 0.3);
    }

        .btn-primary:hover {
            background: var(--gold);
            color: var(--dark-navy);
            border-color: var(--gold);
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(201, 169, 97, 0.5);
        }

    .btn-outline-primary {
        border: 2px solid var(--dark-navy);
        color: var(--dark-navy);
        background: transparent;
    }

        .btn-outline-primary:hover {
            background: var(--dark-navy);
            color: var(--gold);
        }

    .btn-success {
        background: linear-gradient(135deg, #2E7D32 0%, #1B5E20 100%);
        color: white;
    }

    .btn-warning {
        background: var(--gold);
        color: var(--dark-navy);
    }

    .btn-danger {
        background: linear-gradient(135deg, #8B0000 0%, #5C0000 100%);
        color: white;
    }

    /* ========== BADGES LUXE ========== */
    .badge {
        padding: 10px 20px;
        border-radius: 0;
        font-weight: 500;
        font-size: 0.8rem;
        letter-spacing: 1.5px;
        text-transform: uppercase;
    }

    .bg-success {
        background: linear-gradient(135deg, #2E7D32 0%, #1B5E20 100%) !important;
        box-shadow: 0 2px 10px rgba(46, 125, 50, 0.3);
    }

    /* ========== HERO PRESTIGE ========== */
    .hero-section {
        background: linear-gradient(135deg, var(--dark-navy) 0%, #000000 100%);
        padding: 140px 0;
        text-align: center;
        position: relative;
        overflow: hidden;
        border-bottom: 5px solid var(--gold);
        box-shadow: inset 0 0 100px rgba(201, 169, 97, 0.1);
    }

        .hero-section::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(201, 169, 97, 0.05) 1px, transparent 1px);
            background-size: 50px 50px;
            animation: moveBackground 60s linear infinite;
        }

    @@keyframes moveBackground {
        0% {
            transform: translate(0, 0);
        }

        100% {
            transform: translate(50px, 50px);
        }
    }

    .hero-section::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: linear-gradient(90deg, transparent 0%, var(--gold) 50%, transparent 100%);
        box-shadow: 0 0 30px var(--gold);
    }

    .hero-content {
        position: relative;
        z-index: 1;
    }

        .hero-content h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 5rem;
            font-weight: 600;
            margin-bottom: 2rem;
            letter-spacing: 4px;
            text-shadow: 0 0 30px rgba(201, 169, 97, 0.5);
            line-height: 1.2;
        }

    .brand {
        font-weight: 300;
        color: white;
    }

        .brand strong {
            color: var(--gold);
            font-weight: 700;
            text-shadow: 0 0 20px var(--gold);
        }

    .lead {
        font-size: 1.5rem;
        opacity: 0.9;
        margin-bottom: 3rem;
        font-weight: 300;
        letter-spacing: 2px;
        color: rgba(255, 255, 255, 0.95);
    }

    /* ========== FORMS ÉLÉGANTS ========== */
    .form-control, .form-select {
        border-radius: 0;
        border: none;
        border-bottom: 2px solid #E5E5E5;
        padding: 14px 0;
        transition: all 0.3s;
        font-size: 0.95rem;
        background: transparent;
    }

        .form-control:focus, .form-select:focus {
            border-bottom-color: var(--gold);
            box-shadow: 0 2px 0 var(--gold);
            outline: none;
            background: transparent;
        }

    .form-label {
        font-weight: 500;
        color: var(--dark-navy);
        margin-bottom: 1rem;
        font-size: 0.9rem;
        letter-spacing: 1px;
        text-transform: uppercase;
    }

    /* ========== ALERTS PRESTIGE ========== */
    .alert {
        border-radius: 0;
        border: none;
        border-left: 4px solid;
        padding: 1.5rem 2rem;
        font-weight: 400;
        box-shadow: 0 2px 15px rgba(0, 0, 0, 0.08);
    }

    .alert-success {
        background: linear-gradient(90deg, #E8F5E9 0%, white 100%);
        color: #2E7D32;
        border-left-color: #2E7D32;
    }

    .alert-danger {
        background: linear-gradient(90deg, #FFEBEE 0%, white 100%);
        color: #8B0000;
        border-left-color: #8B0000;
    }

    /* ========== SCROLLBAR PRESTIGE ========== */
    ::-webkit-scrollbar {
        width: 14px;
    }

    ::-webkit-scrollbar-track {
        background: var(--cream);
        border-left: 1px solid #E5E5E5;
    }

    ::-webkit-scrollbar-thumb {
        background: linear-gradient(180deg, var(--dark-navy) 0%, var(--gold) 100%);
        border: 2px solid var(--cream);
    }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--gold);
        }

    /* ========== ANIMATIONS ========== */
    @@keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .card {
        animation: fadeInUp 0.6s ease-out;
    }

    /* ========== RESPONSIVE ========== */
    @@media (max-width: 768px) {
        .hero-content h1 {
            font-size: 3rem;
        }

        .lead {
            font-size: 1.2rem;
        }
    }

    /* ========== DARK MODE ========== */
    body.dark-mode {
        background: #0B1A2E;
    }

        body.dark-mode main {
            background: #0B1A2E;
        }

            body.dark-mode main > .top-row {
                background: #152238;
                border-bottom: 1px solid #C9A961;
            }

        body.dark-mode .content {
            background: #0B1A2E;
        }

        body.dark-mode .card {
            background: #152238;
            border: 1px solid rgba(201, 169, 97, 0.2);
        }

        body.dark-mode .card-body {
            background: #152238;
        }

        body.dark-mode .card-footer {
            background: #0B1A2E;
            border-top: 1px solid rgba(201, 169, 97, 0.2);
        }

        body.dark-mode .card-title,
        body.dark-mode h1,
        body.dark-mode h2,
        body.dark-mode h3 {
            color: #C9A961;
        }

        body.dark-mode .card-text,
        body.dark-mode p {
            color: rgba(255, 255, 255, 0.8);
        }

        body.dark-mode .form-control,
        body.dark-mode .form-select {
            background: #152238;
            color: white;
            border-bottom-color: rgba(201, 169, 97, 0.3);
        }

            body.dark-mode .form-control:focus,
            body.dark-mode .form-select:focus {
                background: #152238;
                border-bottom-color: #C9A961;
            }

        body.dark-mode .form-label {
            color: #C9A961;
        }

        body.dark-mode .alert-success {
            background: linear-gradient(90deg, rgba(46, 125, 50, 0.2) 0%, #152238 100%);
            color: #4CAF50;
        }

        body.dark-mode .alert-danger {
            background: linear-gradient(90deg, rgba(139, 0, 0, 0.2) 0%, #152238 100%);
            color: #FF6B6B;
        }

        body.dark-mode ::-webkit-scrollbar-track {
            background: #0B1A2E;
        }
</style>